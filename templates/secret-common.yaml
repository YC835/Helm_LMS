apiVersion: v1
kind: Secret
metadata:
  name: common-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.commonSecret }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
